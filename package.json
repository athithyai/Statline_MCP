{
  "name": "mcp-statline",
  "version": "0.1.0",
  "description": "MCP server for CBS StatLine — search and query Statistics Netherlands open data tables",
  "license": "MIT",
  "type": "module",
  "bin": { "mcp-statline": "dist/index.js" },
  "main": "dist/index.js",
  "files": ["dist", "README.md", "LICENSE"],
  "engines": { "node": ">=18" },
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "start": "node dist/index.js",
    "prepare": "npm run build"
  },
  "keywords": ["mcp", "cbs", "statline", "opendata", "statistics", "netherlands", "odata"],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
